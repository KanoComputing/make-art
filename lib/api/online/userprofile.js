"use strict";
var api, sdk;


function userProfile(config) {
    // sdk = sdkFactory(config);
    // api = sdk.api;

    // api.add('getProfile', {
    //     method : 'get',
    //     route  : '/users/detail/:userId',
    //     params  : ['userId']
    // });

    // return api;
}

export default userProfile;

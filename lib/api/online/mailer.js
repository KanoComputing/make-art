"use strict";

function mailer(config) {
    // var api = sdkFactory(config).api;

    // api.add('mailer', {
    //     method : 'post',
    //     route  : '/mail'
    // });

    // return api.mailer;
}

export default mailer;

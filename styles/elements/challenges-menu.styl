challenge-button-size = 50px
challenge-button-gutter = 10px

.modal-inner.challenges-menu

    ul.challenges-list
        debullet()
        padding: 20px 90px
        clearfix()

        li
            position relative
            margin (challenge-button-gutter / 2)
            float left

            .button
                width challenge-button-size
                height challenge-button-size
                line-height challenge-button-size
                padding 0
                margin 0
                text-align center
                font-weight bold
                font-size 18px

                &.unlocked
                    button-color(saturate(darken(color-green, 6), 10))
                    &.lastUnlocked
                        button-color(color-blue)

                &.locked
                    button-color(#dddddd)

                &.playground
                    button-color(saturate(darken(color-green, 6), 10))
                    width (3 * challenge-button-size) + (2 * challenge-button-gutter)
                &:hover
                    &.unlocked
                        button-color(lighten(color-green, 5))
                        &.lastUnlocked
                            button-color(lighten(color-blue, 5))
                    &.playground
                        button-color(saturate(darken(color-green, 1), 10))
                    & ~ .challenge-title
                        display block

                & ~ .challenge-title
                    display none
                    background #44423d
                    color #ffffff
                    padding 10px
                    font-weight bold
                    font-size 14px
                    line-height 14px
                    white-space nowrap
                    border-radius 3px
                    bottom 65px

                    &:after
                        content ''
                        display block
                        bottom -15px
                        width 0
                        height 0
                        border-left 15px solid transparent
                        border-right 15px solid transparent
                        border-top 15px solid #44423d

                    &,
                    &:after
                        position absolute
                        left 50%
                        -webkit-transform translate(-50%, 0)
                        transform translate(-50%, 0)

editor .controls
    position absolute
    z-index 1
    right 10px
    bottom 10px
    z-index 8

    .button
        button-color(lighten(color-editor-bg, 10))
        width 40px
        height @width
        line-height @width
        text-align center
        padding 0